# Copy to .env and fill in real values
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
REDIS_USERNAME=default

# Must match todo-backend BULLMQ_QUEUE
BULLMQ_QUEUE=todo

# Jobs processed in parallel per worker process
WORKER_CONCURRENCY=5
